Bootstrap: docker
From: mambaorg/micromamba:1.5.8

%post
    micromamba create -y -n tai_python \
        -c conda-forge \
        -c bioconda \
        python=3.11 \
        pandas \
        biopython

    micromamba clean -a -y

%environment
    export PATH=/opt/conda/envs/tai_python/bin:$PATH